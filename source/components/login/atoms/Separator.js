import React from 'react';
import { Text } from 'react-native';

const Separator = ({ content }) =>
    <Text style={{ color: 'gray', fontSize: 18, marginVertical: 15 }}>{content}</Text>;

export default Separator;
